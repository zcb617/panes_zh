# Panes 电脑操作 SDK 集成测试清单

## 1. 测试目的

本清单从 Panes 的真实业务闭环倒推测试，不以“原生库能加载”作为功能通过条件。每个用例都必须验证一条完整链路：

```text
模型发起电脑操作工具调用
        ↓
Panes 识别任务、引擎、目标窗口和操作范围
        ↓
Panes 在首次需要权限时弹出授权申请
        ↓
用户允许或拒绝
        ↓
Panes 调用 CUA Driver SDK
        ↓
真实窗口发生预期变化
        ↓
结果或错误回到模型
```

测试结论必须同时包含：业务结果、授权结果、SDK 调用结果、窗口实际变化、进程状态和日志关联关系。只看到接口返回成功，不能判定电脑操作成功。

当前执行状态：阶段 0、阶段 1、阶段 2、阶段 3 已有分阶段执行记录；Windows GUI 实机、Claude/OpenCode 真实会话、设置页迁移和跨平台验收仍未完成。跨阶段未完成事项以《[电脑操作 SDK 未完成清单](<电脑操作SDK未完成清单.md>)》为准。

阶段开始和收尾必须回看该主清单：属于当前阶段且已经具备验证条件的事项，不得继续留在“后续回归”，必须在当前阶段补测并把过程、结果和证据写回本清单。

## 2. 测试边界和准备条件

### 2.1 测试对象

| 项目 | 约束 |
| --- | --- |
| Panes | `codex/computer-control-sdk` 分支的构建产物 |
| 首个目标平台 | Windows |
| 官方 SDK | 固定一个官方 release 版本，使用发布包内的 `cua_driver_sdk.dll`、`cua_driver_abi.h` 及其依赖 |
| 测试窗口 | Windows 记事本作为安全输入窗口；另准备一个可重复操作的业务演示窗口 |
| 引擎 | Codex、Claude Code、OpenCode 各至少一个新会话 |
| 记录方式 | 每个用例填写实际步骤、时间、结果、截图或日志位置、`request_id` |

### 2.2 测试前检查

- 确认测试包中的原生库来自同一个官方 release archive。
- 确认 Panes 从安装包或 Tauri resources 相对目录加载库，不依赖 PATH。
- 确认测试机没有旧的 `--panes-computer-control-mcp` 进程；如有，先记录为环境问题，不把它归因于新实现。
- 准备一个不会造成真实业务损失的测试窗口，禁止直接用生产 ERP 做首次输入测试。
- 每轮测试开始前清空上一轮的临时授权和测试任务。

## 3. 业务能力到测试范围的映射

| 业务编号 | Panes 业务能力 | 对应测试组 |
| --- | --- | --- |
| B01 | 在 Panes 设置中独立开关电脑操作能力 | G1 |
| B02 | 模型真正要操作时申请授权，不预先添加 exe | G2 |
| B03 | 模型观察窗口、截图和窗口状态 | G3 |
| B04 | 模型在已授权窗口中执行鼠标键盘操作 | G4 |
| B05 | 模型请求启动应用时单独确认 | G5 |
| B06 | 授权只覆盖当前任务、目标应用、目标窗口和操作范围 | G6 |
| B07 | 关闭、取消、断开和退出时立即释放能力 | G7 |
| B08 | Codex、Claude Code、OpenCode 都通过同一个 Panes 服务调用 | G8 |
| B09 | 官方预编译 SDK 能随 Panes 正确加载和退出 | G9 |
| B10 | 不再启动 MCP、第二个 Panes 或黑框检测进程 | G10 |

## 4. 执行顺序和阻断规则

测试必须按以下顺序执行，前一组未通过不得跳到后一组：

1. G9：原生库、ABI、资源路径和 runtime 生命周期。
2. G1：设置开关和惰性初始化。
3. G2：首次真实工具调用和授权弹窗。
4. G3、G4、G5：观察、输入和启动应用的业务动作。
5. G6：越权、目标失效和拒绝场景。
6. G7：关闭、取消、断开和重启清理。
7. G8：三个引擎的工具注册和结果回传。
8. G10：进程、路径、黑框和回归检查。

以下任一情况出现，立即暂停后续测试并记录阻断原因：

- 真实窗口发生了授权范围之外的动作。
- 用户拒绝后仍然执行了鼠标、键盘或启动动作。
- 关闭开关或退出 Panes 后仍能继续调用 SDK。
- 出现第二个 Panes、`--panes-computer-control-mcp` 或无法回收的 SDK worker。
- 截图、文本或按键结果与真实窗口不一致。
- 测试过程无法用同一个 `request_id` 关联模型调用、授权、SDK 执行和结果。

## 5. 集成测试用例

每个用例的“实际结果、状态、证据”必须在执行时填写。状态只允许使用：`通过`、`失败`、`阻断`、`未执行`。

### G1：设置开关和初始化

| 编号 | 目的 | 前置条件 | 操作步骤 | 预期结果 | 实际结果 / 状态 / 证据 |
| --- | --- | --- | --- | --- | --- |
| CT-G1-01 | 验证电脑操作是 Panes 独立功能 | Panes 启动 | 打开设置页面，查看电脑操作区块和 MCP 区块 | 电脑操作能力独立展示，不出现在左上角 MCP 列表 | 待填写 |
| CT-G1-02 | 验证默认关闭不加载 runtime | 新安装或清空配置 | 启动 Panes，保持开关关闭，查看 SDK 状态和进程 | 状态为已关闭；没有加载 DLL、没有 SDK worker、没有检测进程 | 待填写 |
| CT-G1-03 | 验证打开开关不提前执行电脑动作 | 开关关闭 | 打开“允许电脑操作”，停留设置页 30 秒 | 状态变为未初始化或可用；不弹授权、不动鼠标、不产生黑框、不循环检测 | 待填写 |
| CT-G1-04 | 验证惰性初始化只发生一次 | 开关已打开 | 不调用工具，随后连续进入和离开设置页 | 不因页面进入重复初始化；没有“检测中”长时间停留 | 待填写 |
| CT-G1-05 | 验证关闭开关立即失效 | 存在一个已授权任务 | 关闭开关，再让同一任务调用电脑工具 | 调用返回 `computer_control_disabled`，不执行真实动作 | 待填写 |

### G2：模型首次调用和授权申请

| 编号 | 目的 | 前置条件 | 操作步骤 | 预期结果 | 实际结果 / 状态 / 证据 |
| --- | --- | --- | --- | --- | --- |
| CT-G2-01 | 验证首次真实调用才申请授权 | 开关已打开；没有 exe 白名单 | 让模型调用 `list_windows` 或 `get_window_state` | 调用到达 Panes 后才弹授权；设置页本身不弹授权 | 待填写 |
| CT-G2-02 | 验证授权内容可判断 | CT-G2-01 弹窗出现 | 查看弹窗中的引擎、任务、目标应用、目标窗口、操作范围和模型意图 | 用户能在执行前看懂“谁要操作哪个窗口、做什么” | 待填写 |
| CT-G2-03 | 验证拒绝授权不会执行动作 | 弹出输入类授权 | 点击拒绝，观察窗口、鼠标、键盘和工具结果 | 无真实动作；返回 `permission_denied`；不自动再次弹窗 | 待填写 |
| CT-G2-04 | 验证允许后只授权当前范围 | 弹出观察类授权 | 允许“查看指定窗口”，随后调用观察工具 | 观察调用成功；输入和启动应用仍需重新申请 | 待填写 |
| CT-G2-05 | 验证任务绑定 | 一个任务已获得授权 | 新建另一个任务，调用同一目标窗口 | 新任务不能继承旧任务授权，必须重新申请 | 待填写 |
| CT-G2-06 | 验证没有现成 exe 时仍能申请 | 目标窗口已运行但无法提前配置路径 | 从模型发起窗口观察调用 | 弹窗展示 SDK 能识别的实际应用和窗口信息，不要求用户先添加 exe | 待填写 |

### G3：观察类业务动作

| 编号 | 目的 | 前置条件 | 操作步骤 | 预期结果 | 实际结果 / 状态 / 证据 |
| --- | --- | --- | --- | --- | --- |
| CT-G3-01 | 验证窗口发现 | 已允许观察目标应用 | 调用 `list_windows` | 返回窗口标题、进程、应用路径和受控窗口标识；不返回可直接伪造的裸句柄 | 待填写 |
| CT-G3-02 | 验证窗口状态和截图 | 已获得 `target_window` | 调用 `get_window_state` | 返回尺寸、前台状态、UIA 摘要和模型可读截图；截图与真实窗口一致 | 待填写 |
| CT-G3-03 | 验证等待状态变化 | 目标窗口可重复变化 | 调用 `wait`，再让窗口标题或内容发生变化 | 在超时范围内返回变化或明确超时，不阻塞整个引擎任务 | 待填写 |
| CT-G3-04 | 验证观察不隐式升级权限 | 仅授权观察 | 让模型连续调用窗口和截图工具 | 不移动鼠标、不输入键盘、不启动应用 | 待填写 |

### G4：鼠标和键盘业务动作

测试使用专用记事本或演示窗口，输入内容必须是无敏感信息的固定文本。

| 编号 | 目的 | 前置条件 | 操作步骤 | 预期结果 | 实际结果 / 状态 / 证据 |
| --- | --- | --- | --- | --- | --- |
| CT-G4-01 | 验证前台窗口切换 | 已授权目标窗口 | 调用 `focus_window` | 只有授权窗口被置前；其他窗口不被操作 | 待填写 |
| CT-G4-02 | 验证单击 | 已授权目标窗口和坐标 | 调用 `click` | 只在目标窗口指定位置单击一次；结果可由截图确认 | 待填写 |
| CT-G4-03 | 验证双击 | 已授权目标窗口和坐标 | 调用 `double_click` | 只发生一次双击；不能被重复执行 | 待填写 |
| CT-G4-04 | 验证文本输入 | 已授权目标窗口的文本框 | 调用 `type_text` 输入固定测试文本 | 文本只进入目标文本框；日志不记录完整文本内容 | 待填写 |
| CT-G4-05 | 验证受限按键 | 已授权目标窗口 | 调用 `key_press` 发送一个允许的按键组合 | 只发送被允许的按键；不接受任意脚本或任意命令 | 待填写 |
| CT-G4-06 | 验证滚动 | 已授权目标窗口 | 调用 `scroll` | 只滚动目标窗口；滚动量与结果可由截图确认 | 待填写 |
| CT-G4-07 | 验证动作结果回传 | 任意输入动作完成 | 让模型继续下一步观察 | 模型收到动作完成结果和最新状态，不能只收到无依据的“成功” | 待填写 |

### G5：启动应用业务动作

| 编号 | 目的 | 前置条件 | 操作步骤 | 预期结果 | 实际结果 / 状态 / 证据 |
| --- | --- | --- | --- | --- | --- |
| CT-G5-01 | 验证启动应用是单独高风险权限 | 开关已打开；目标程序未运行 | 让模型调用 `launch_app` | 弹窗明确展示启动路径和原因，不能沿用观察权限直接启动 | 待填写 |
| CT-G5-02 | 验证启动路径不是写死路径 | 使用测试机上的合法测试程序 | 通过模型请求启动该程序 | Panes 使用用户确认的路径或受控路径，不读取开发机绝对路径 | 待填写 |
| CT-G5-03 | 验证拒绝启动 | `launch_app` 授权弹窗出现 | 点击拒绝 | 程序不启动，返回 `permission_denied` | 待填写 |
| CT-G5-04 | 验证启动结果可观察 | 允许启动测试程序 | 启动后调用 `list_windows` 和 `get_window_state` | 返回新窗口的受控标识；后续输入还需按目标窗口授权 | 待填写 |

### G6：授权边界和异常目标

| 编号 | 目的 | 前置条件 | 操作步骤 | 预期结果 | 实际结果 / 状态 / 证据 |
| --- | --- | --- | --- | --- | --- |
| CT-G6-01 | 验证窗口标识不能伪造 | 已获得一个目标窗口标识 | 修改请求中的窗口标识或使用过期标识 | 返回 `target_not_found` 或参数错误，不退化成全局坐标操作 | 待填写 |
| CT-G6-02 | 验证目标窗口关闭后的处理 | 已获得目标窗口授权 | 关闭目标窗口，再调用输入工具 | 返回 `target_not_found`，不操作其他窗口 | 待填写 |
| CT-G6-03 | 验证切换应用需要重新授权 | 已授权应用 A | 让模型请求操作应用 B | 弹出新的授权申请；不能继承应用 A 的授权 | 待填写 |
| CT-G6-04 | 验证观察升级到输入需要重新授权 | 只授权观察 | 让模型调用 `click` 或 `type_text` | 返回 `authorization_required` 并弹输入授权 | 待填写 |
| CT-G6-05 | 验证输入升级到启动需要重新授权 | 已授权输入 | 让模型调用 `launch_app` | 返回 `authorization_required` 并弹高风险授权 | 待填写 |
| CT-G6-06 | 验证禁止能力不会暴露 | 任意任务 | 尝试请求 shell、任意文件读写、进程终止、注册表修改和无目标全局点击 | 工具目录没有这些工具；请求被明确拒绝 | 待填写 |
| CT-G6-07 | 验证敏感数据不进入日志 | 执行文本输入 | 输入固定的模拟敏感字符串 | 日志只保留工具、目标摘要、耗时和 `request_id`，不保存完整文本或原始截图 | 待填写 |

### G7：任务、引擎和 Panes 生命周期

| 编号 | 目的 | 前置条件 | 操作步骤 | 预期结果 | 实际结果 / 状态 / 证据 |
| --- | --- | --- | --- | --- | --- |
| CT-G7-01 | 验证授权等待可取消 | 授权弹窗等待中 | 取消当前任务 | 弹窗关闭，请求返回 `cancelled`，没有 SDK 动作 | 待填写 |
| CT-G7-02 | 验证关闭开关可取消授权 | 授权弹窗等待中 | 关闭电脑操作开关 | 等待中的请求取消，临时授权全部撤销 | 待填写 |
| CT-G7-03 | 验证引擎断开可回收 | 已有授权或执行中请求 | 断开当前引擎 | 请求返回 `engine_disconnected`，该引擎授权撤销，SDK 不继续执行 | 待填写 |
| CT-G7-04 | 验证任务结束回收 | 任务已完成 | 结束任务后再次调用电脑工具 | 旧授权失效；新任务必须重新申请 | 待填写 |
| CT-G7-05 | 验证重复开关不重复创建 runtime | 开关反复开关 | 连续打开、关闭、打开并查看 runtime 和进程 | 不出现 `runtime_already_exists`、重复 worker 或多个 SDK runtime | 待填写 |
| CT-G7-06 | 验证正常退出清理 | 有活动授权和 SDK runtime | 退出 Panes | 授权撤销，`shutdown` 完成，SDK 私有 worker 如存在则退出 | 待填写 |
| CT-G7-07 | 验证重启后不恢复授权 | 上一轮已退出 Panes | 重新启动 Panes，使用同一目标窗口 | 不恢复旧授权、窗口标识和进程标识；需重新授权 | 待填写 |

### G8：三个引擎的真实接入

| 编号 | 目的 | 前置条件 | 操作步骤 | 预期结果 | 实际结果 / 状态 / 证据 |
| --- | --- | --- | --- | --- | --- |
| CT-G8-01 | 验证 Codex 新线程工具注册 | Codex app-server 可用 | 新建线程，让模型调用电脑工具 | `dynamicTools` 包含 Panes 工具；调用带有 `threadId`、`turnId`、`callId` 并能回传结果 | 待填写 |
| CT-G8-02 | 验证 Codex 历史线程行为 | 打开没有 Panes 工具定义的旧线程 | 尝试调用电脑工具 | 不静默失败；UI 明确提示需要新建会话 | 待填写 |
| CT-G8-03 | 验证 Claude 自定义工具 | Claude sidecar 可用 | 新建任务，让模型调用观察和输入工具 | 工具通过 sidecar 转发到同一个 Panes 服务，授权和结果与 Codex 一致 | 待填写 |
| CT-G8-04 | 验证 OpenCode 自定义工具 | OpenCode server 使用 Panes 专属目录 | 新建任务，让模型调用截图和输入工具 | 不修改用户全局配置；工具结果和图片格式符合当前适配器约定 | 待填写 |
| CT-G8-05 | 验证引擎隔离 | Codex、Claude、OpenCode 同时存在 | 分别创建任务并授权不同目标 | 授权按任务和引擎隔离，不能串用 `request_id` 或窗口权限 | 待填写 |

### G9：官方预编译 SDK 和平台运行时

| 编号 | 目的 | 前置条件 | 操作步骤 | 预期结果 | 实际结果 / 状态 / 证据 |
| --- | --- | --- | --- | --- | --- |
| CT-G9-01 | 验证 Windows 官方库加载 | Windows 官方 archive 已放入 resources | 启动开关、首次调用、执行 `shutdown` | DLL、ABI、架构和依赖校验通过；runtime 创建和退出成功 | 待填写 |
| CT-G9-02 | 验证缺失依赖的错误可见 | 移走一个测试依赖 | 执行首次调用 | 返回 `sdk_unavailable` 和具体原因，不循环检测、不启动黑框 | 待填写 |
| CT-G9-03 | 验证资源相对路径 | 在不同安装目录运行同一包 | 执行首次调用 | 不依赖开发机路径、当前工作目录或 PATH | 待填写 |
| CT-G9-04 | 验证 ABI 版本不匹配 | 使用不匹配的测试库 | 执行首次调用 | 启动前明确失败，不执行鼠标键盘动作 | 待填写 |
| CT-G9-05 | 验证 Windows 私有 worker 生命周期 | 目标官方版本声明需要 UIAccess worker | 创建 runtime、执行一次调用、关闭开关、退出 Panes | worker 只由 SDK runtime 管理，关闭后退出，不变成常驻服务 | 待填写 |
| CT-G9-06 | 验证 macOS 发布库 | macOS 对应 release archive | 执行库加载、创建、一次观察、shutdown 烟测 | 使用 `.dylib` 和对应权限；不把 Windows 路径或进程模型带入 macOS | 待填写 |
| CT-G9-07 | 验证 Linux 发布库 | Linux 对应 release archive | 执行库加载、创建、一次观察、shutdown 烟测 | 使用 `.so` 和对应桌面权限；不把 Windows 路径或进程模型带入 Linux | 待填写 |

### G10：进程、黑框和回归

| 编号 | 目的 | 前置条件 | 操作步骤 | 预期结果 | 实际结果 / 状态 / 证据 |
| --- | --- | --- | --- | --- | --- |
| CT-G10-01 | 验证不启动 MCP 代理 | Panes 正常运行 | 打开设置、打开开关、执行一次真实工具调用 | 进程命令行没有 `--panes-computer-control-mcp`，没有 `cua-driver mcp` | 待填写 |
| CT-G10-02 | 验证不启动第二个 Panes | Panes 正常运行 | 重复进入设置、重复执行工具、切换三个引擎 | 只有用户启动的 Panes 进程；不出现第二个 Panes 宿主 | 待填写 |
| CT-G10-03 | 验证无黑框闪现 | Windows 桌面可见 | 进入设置、开关切换、首次调用和关闭 | 不出现命令行窗口闪现；授权弹窗是 Panes UI | 待填写 |
| CT-G10-04 | 验证无“检测中”死循环 | SDK 可用和不可用各测一次 | 进入设置并等待状态稳定 | 状态在已关闭、未初始化、可用、失败或不支持之间稳定收敛 | 待填写 |
| CT-G10-05 | 验证旧配置不重新启用 MCP | 存在旧版 broker 或白名单字段 | 启动新版 Panes 并执行工具调用 | 旧字段只兼容读取，不修改全局配置，不自动恢复 MCP 或 exe 白名单 | 待填写 |
| CT-G10-06 | 验证重复调用和异常恢复 | 连续执行成功、拒绝、超时和目标失效 | 再次执行合法调用 | 每次都有独立 `request_id`；失败请求不会污染下一次授权和目标窗口 | 待填写 |

## 6. 单条用例的执行记录格式

每个用例执行时，必须复制下面模板填写，不允许只写“通过”或“失败”：

```text
用例编号：
执行时间：
执行人：
Panes 版本 / 分支：
CUA release 版本：
平台和系统版本：
引擎及会话：
前置条件：
实际操作步骤：
request_id：
授权结果：允许 / 拒绝 / 未申请 / 被撤销
SDK 调用结果：
真实窗口变化：
进程变化：
日志或截图证据：
最终状态：通过 / 失败 / 阻断 / 未执行
失败原因或后续动作：
```

## 7. 最终通过条件

只有同时满足以下条件，才可以宣布“电脑操作 SDK 集成测试通过”：

- G1 至 G10 的必测用例全部有实际记录，没有“未执行”项被误当作通过。
- 首次真实工具调用前不申请授权、不加载 runtime、不启动检测进程。
- 用户拒绝、关闭开关、任务结束、引擎断开和 Panes 退出都能阻断后续动作并回收临时授权。
- 观察、输入、启动应用分别遵守授权范围，越权调用不会退化成全局鼠标或键盘操作。
- Codex、Claude Code、OpenCode 都能通过同一个 Panes 电脑操作服务完成授权、执行和结果回传。
- Windows、macOS、Linux 使用各自官方预编译库；Panes 不编译 CUA 源码，不依赖写死路径。
- 不存在 `cua-driver mcp`、`--panes-computer-control-mcp`、第二个 Panes 或设置页黑框闪现。
- 每条测试链路都能用 `request_id` 从模型调用追踪到授权、SDK 执行、真实窗口变化和最终结果。

## 8. 阶段 0 已执行记录

执行时间：2026-08-11 18:53（Asia/Hong_Kong）

执行对象：官方 `cua-driver-rs-v0.19.3` Windows x86_64 发布包。

发布包 SHA256：`e48b0117e343cec2577fc12693c741e094f389f8d4aef91e06284960bb03bce1`

原生库 SHA256：`d7e67baef87fac1a315d86113eeb485fbf8e03e5906797f0bf79d24a990fa38b`

### CT-G9-01：Windows 官方库加载

- 实际步骤：下载官方 zip；确认包内有 `cua_driver_sdk.dll` 和 `cua_driver_abi.h`；使用 Spike 动态加载 DLL。
- 实际输出：`ABI status=0 version=1.1.0 compatible=true rejects_next_major=true`。
- 实际输出：`CREATE status=0`、`AVAILABLE status=0 value=true`。
- 实际输出：metadata 中 `driver_version=0.19.3`、`embedded=true`。
- 结果：`通过`。

### CT-G9-02：缺失库时错误可见

- 实际步骤：使用不存在的 DLL 路径启动 Spike。
- 实际输出：`SPIKE_FAILED failed to load native library`，退出码为 1。
- 结果：`通过`。没有循环检测，也没有启动命令行窗口。
- 备注：缺失“依赖文件”的 Panes 安装包测试，待资源目录接入后补测。

### CT-G9-03：相对资源路径

- 实际步骤：从 `.spike-assets` 工作目录，以相对路径加载同一 DLL。
- 实际输出：ABI、CREATE、AVAILABLE、GET_SCREEN_SIZE、SHUTDOWN 全部成功，退出码为 0。
- 结果：`通过（Spike）`。
- 备注：Panes Tauri resources resolver 尚未接入，安装目录变更测试待补测。

### CT-G9-04：ABI 版本边界

- 实际步骤：读取 DLL ABI 版本，并同时检查当前版本兼容性和下一主版本兼容性。
- 实际输出：当前 `1.1` 返回 `compatible=true`，下一主版本返回 `rejects_next_major=true`。
- 结果：`通过（Spike）`。
- 备注：使用错误 DLL 文件的完整启动阻断测试待 Panes 资源加载层接入后补测。

### CT-G9-05：Windows 私有 worker 生命周期

- 实际步骤：记录 Spike 前后 `cua-driver` 进程数量；创建 runtime、执行一次桌面状态读取、调用 shutdown。
- 实际输出：`CUA_PROCESS_COUNT before=1 after=1`；metadata 标记 `embedded=true`；shutdown 返回状态 0。
- 结果：`通过（本轮无新增进程）`。
- 备注：测试机在执行前已有一个同名进程，当前权限无法读取其命令行，未对它做任何结束操作；因此不能把它归因于本轮 Spike。

### CT-G9-06 / CT-G9-07：macOS、Linux 发布库

- 结果：`未执行`。
- 原因：当前 Spike 环境为 Windows；待对应平台 CI 产物和实机测试环境准备后执行。

### 阶段 0 总结

Windows 官方预编译 SDK 已完成“加载 → ABI → runtime 创建 → 工具目录 → 桌面状态读取 → shutdown”闭环，Spike 通过。下一步才进入 Panes resources 正式集成；在此之前不接设置 UI，也不接 Codex、Claude Code、OpenCode。

## 9. 阶段 1 已执行记录：Panes 正式 runtime

执行时间：2026-08-11 19:00 左右（Asia/Hong_Kong）

### CT-G9-01：Panes 主进程正式模块加载

- 实际改动：新增 `src-tauri/src/computer_control_sdk.rs`，使用稳定 C ABI 动态加载官方 DLL；新增 Tauri managed state 和 5 个内部命令入口。
- 实际步骤：设置 `PANES_CUA_SDK_SPIKE=1`，执行 Panes Rust 单元测试 `computer_control_sdk::tests::official_windows_release_runtime_smoke`。
- 实际结果：runtime 初始化、ABI、metadata、工具目录、`get_screen_size`、shutdown 全部成功。
- 结果：`通过`。

### CT-G9-03：Panes resources 资源路径

- 实际改动：官方 Windows 包复制到 `src-tauri/resources/cua-driver/windows-x86_64`；Tauri resources 配置增加 `resources/cua-driver`；新增版本和哈希清单。
- 实际步骤：解析 `tauri.conf.json`，确认资源目录存在并包含 DLL、ABI 头文件和官方配套组件。
- 实际结果：资源配置解析成功，资源目录和 manifest 均存在；路径解析不依赖开发机绝对路径。
- 结果：`通过（资源层）`。

### 阶段 1 编译结果

- Rust 静态检查：通过；新增 runtime 模块没有编译错误。
- 定向 Rust 单元测试：`1 passed, 0 failed`。
- 当前仍未启动 Panes GUI，因此设置 UI、授权弹窗和三个引擎用例保持未执行。

## 10. 阶段 2 已执行记录：统一服务和 Codex 工具接入

执行时间：2026-08-11（Asia/Hong_Kong）

执行分支：`codex/computer-control-sdk`，工作树：`.worktree/computer-control-sdk`

### CT-G8-01 / S2-01：Codex 新线程动态工具注册

- 实际改动：新增 `ComputerControlService::dynamic_tools_spec()`；`thread/start` 参数加入 `dynamicTools`，命名空间为 `panes_computer_control`。
- 实际步骤：执行 Rust 静态检查（包含测试构建），并增加 `thread_start_params_register_panes_computer_control_namespace` 测试。
- 实际结果：测试代码构建通过；当前 Windows 测试宿主在运行阶段报告系统入口点缺失，未把 GUI 外的单测运行结果记为通过。
- 最终状态：`阻断（运行时单测宿主）`。

### S2-02：关闭开关时不初始化 SDK

- 实际改动：统一服务先读取 Panes 电脑操作开关，再进入授权和 SDK `invoke`；动态工具目录生成不触发 SDK 初始化。
- 实际结果：静态检查通过；尚未从设置页切换开关并执行真实 Codex 调用。
- 最终状态：`未执行（待设置页迁移阶段）`。

### S2-03 / S2-04 / S2-05：授权、拒绝和允许后的调用

- 实际改动：复用 `computer-control-approval-requested` 事件；新增按线程、回合、目标和操作类型隔离的 pending/grant 状态；允许后才调用 CUA SDK，并返回 `contentItems`、`success`。
- 实际结果：代码构建通过；未启动 Panes GUI，未产生真实 `request_id`、授权窗口或目标应用变化。
- 最终状态：`未执行（待 Codex 实机调用）`。

### S2-06：范围和目标边界

- 实际步骤：新增边界测试，覆盖 `scope=desktop`、无目标点击和普通应用路径。
- 实际结果：测试代码构建通过；静态路径明确拒绝全桌面范围、未审核工具和 Panes 自身目标。
- 最终状态：`代码验证完成，实机未执行`。

### S2-07：取消、回合结束和退出清理

- 实际改动：授权等待监听任务取消；回合结束清理当前回合授权；Codex 连接重置和 Panes 退出撤销全部授权；SDK 在退出阶段显式 shutdown。
- 实际结果：代码构建通过；未执行真实取消、断开和退出场景。
- 最终状态：`代码验证完成，实机未执行`。

### S2-08 / CT-G10-01：进程链约束

- 实际改动：本阶段 Codex 电脑操作不再经过 `--panes-computer-control-mcp`，而是直接由 Panes 统一服务调用已纳入 resources 的 CUA SDK runtime；没有新增 Panes 宿主进程。
- 实际步骤：复用官方 Windows Spike 的 runtime 生命周期和进程计数记录；Spike 创建、调用、shutdown 前后进程数均为 `1`，已有进程未被本轮测试结束。
- 实际结果：官方 Spike 通过；Panes GUI 进程链未在本阶段启动实测。
- 最终状态：`Spike 通过，Panes 实机未执行`。

### 阶段 2 总结

统一服务、授权状态和 Codex 动态工具调用链已经完成并通过 Rust 静态检查；Windows GUI、设置页、授权弹窗和 Notepad 点击/输入/截图闭环必须在后续设置页迁移阶段继续执行，不能以本阶段代码构建结果替代。

## 11. 阶段 3 已执行记录：Claude 和 OpenCode 适配器

执行时间：2026-08-11（Asia/Hong_Kong）

执行分支：`codex/computer-control-sdk`，工作树：`.worktree/computer-control-sdk`

### CT-G8-03：Claude 进程内自定义工具转发

- 实际改动：sidecar 使用 Claude Agent SDK 的 `tool` 和 `createSdkMcpServer` 注册 Panes 工具；Rust 收到 `computer_control_tool_call` 后调用统一 `ComputerControlService`，再发送 `computer_control_tool_result`。
- 实际步骤：使用 sidecar mock 发起 `click` 工具调用，测试桥返回带文本 content 的结果；同时检查工具命名、threadId、turnId 和 callId。
- 实际结果：sidecar 协议测试 18 个通过、1 个平台跳过。
- 最终状态：`代码与协议通过，真实 GUI 会话待回归`。

### CT-G8-04：OpenCode 专属工具目录和本机回调

- 实际改动：每个 OpenCode server 生成独立应用数据临时目录、`.opencode/tools/panes_computer_control.ts` 和随机令牌；进程使用 `OPENCODE_CONFIG_DIR`/`XDG_CONFIG_HOME`，工具通过 `127.0.0.1` 回调进入统一服务。
- 实际步骤：检查本机当前 OpenCode 可执行文件的工具加载契约，确认 `tools/*.{js,ts}`、`description`、`parameters` 和 `execute` 入口；增加工具源生成和工具名归一化测试。
- 实际结果：静态契约和 Rust 编译检查通过；Windows 测试宿主入口点问题已在本阶段定位并修复，相关工具源测试可正常启动。
- 最终状态：`代码与静态契约完成，真实 OpenCode 会话待回归`。

### CT-G8-05 / TODO-013：引擎隔离和 OpenCode 图片结果

- 实际改动：授权 grant key 增加引擎标识；Claude 结果映射保留文本和图片 content；OpenCode 回调原样保留 CUA content。
- 实际结果：代码入口已具备；本阶段未启动真实 OpenCode provider、截图工具和业务窗口，未取得图片 tool-result 证据。
- 最终状态：`未执行，转阶段 5 全引擎视觉回归`。

### CT-G8-06 / TODO-006：Windows 测试宿主入口点定位与修复

- 实际步骤：直接启动 `agent_workspace_lib-*.exe --list` 复现 `0xc0000139`；检查 PE 导入，确认测试宿主引用 `comctl32.dll!TaskDialogIndirect`；检查系统 DLL，确认当前系统 DLL 不导出该入口；检查 Tauri 构建输出，确认 Common Controls v6 清单只链接到正式程序。
- 实际改动：在 Windows 测试构建中复用 Tauri 生成的 `resource.lib`，并通过测试专用静态链接把 Common Controls v6 清单带入库单元测试宿主；正式程序仍使用原有 bin 资源链接，避免重复链接。
- 实际结果：修复后 `agent_workspace_lib-*.exe --list` 退出码为 0，列出 559 个测试；提取到的 manifest 包含 `Microsoft.Windows.Common-Controls` 6.0。正式 `Panes.exe` 构建通过且包含同一清单。
- 补充结果：Windows Rust 全量单元测试已能启动，548 个通过、11 个既有环境敏感用例失败；这些失败不涉及本阶段电脑操作代码，登记为 TODO-023。
- 最终状态：`入口点阻断已修复，剩余全量测试环境项待回归`。

### 阶段 3 总结

Claude 和 OpenCode 已接入同一个 Panes 电脑操作服务，旧 Claude 外部 MCP 配置不再被 sidecar 使用；Windows 测试宿主入口点问题已在本阶段定位并修复。当前仍缺真实授权窗口、目标应用变化、OpenCode 图片结果和三引擎并行回收证据。

补充：完整前端 Vitest 集合本次为 76 个测试文件通过、1 个既有 `pt-BR`/`en` 文案键对齐测试失败；该失败未涉及本阶段文件。
