# 阶段 0：官方预编译 SDK Windows Spike

- 状态：已完成，验证通过。
- 阶段目标：确认官方发布包可以直接使用，不编译 CUA 源码；完成动态库加载、ABI 兼容性检查、驱动创建、工具枚举、屏幕尺寸读取和关闭流程。
- 实际完成：
  - 使用官方发布包 `cua-driver-rs-v0.19.3`。
  - 校验发布压缩包和 Windows x86_64 动态库的 SHA-256。
  - 新增 `spikes/cua-driver-sdk/main.rs` 与说明文档，验证最小调用链。
  - 增加动态库缺失时的失败路径验证。
- 验证方式与结果：
  - ABI 版本：`1.1.0`，兼容。
  - 驱动创建：返回码 `0`。
  - 驱动可用：`true`。
  - 驱动元数据：版本 `0.19.3`，包含内置驱动。
  - 工具枚举：成功。
  - 屏幕尺寸：`1493 x 933`。
  - 关闭流程：返回码 `0`。
  - 缺失动态库：按预期失败，进程退出码 `1`。
  - 进程计数：测试前后均为 `1`；该进程在测试前已存在，未由测试启动，也未被测试结束。
- 阶段结论：官方 Windows 预编译包可以直接加载和调用，Spike 主链路通过；没有编译 CUA 源码的必要。
- 未完成项：当时尚未接入 Panes 正式运行时；尚未验证 macOS/Linux；尚未接入授权业务和 Codex 工具调用。
- 下一阶段入口：把同一套官方运行时资源按相对路径接入 Panes，并由应用生命周期统一管理。
